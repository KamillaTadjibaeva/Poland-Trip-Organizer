% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer.R
\name{solve_tsp}
\alias{solve_tsp}
\title{Solve the (open) Travelling Salesman Problem.}
\usage{
solve_tsp(cost_matrix, start, end = NULL, exact_threshold = 12L)
}
\arguments{
\item{cost_matrix}{Numeric square cost matrix (e.g. \code{\link[=build_cost_matrix]{build_cost_matrix()}}).}

\item{start}{City name or 1-based index of the flight-in city.}

\item{end}{City name or 1-based index of the flight-out city, or \code{NULL}
for an open tour ending wherever is cheapest.}

\item{exact_threshold}{Use exact DP when \code{n <= exact_threshold}.}
}
\value{
A list with elements \code{order} (city names in visit order),
\code{indices} (1-based), \code{cost} (total), \code{method} (\code{"exact"}/\code{"heuristic"}).
}
\description{
Computes the visit order that minimises the total cost, starting from
\code{start} and (optionally) ending at \code{end}. Uses the exact Held–Karp DP
for small instances (n <= \code{exact_threshold}) and falls back to a
nearest-neighbour seed + 2-opt local search for larger ones.
}
