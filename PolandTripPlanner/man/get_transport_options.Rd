% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport.R
\name{get_transport_options}
\alias{get_transport_options}
\title{Fetch transport options for a single leg.}
\usage{
get_transport_options(
  from,
  to,
  date,
  transport = "train",
  style = "fastest",
  cities = NULL
)
}
\arguments{
\item{from, to}{City names.}

\item{date}{Travel \code{Date}.}

\item{transport, style}{See package overview.}

\item{cities}{Cities reference data.frame (used for IATA / coordinates).}
}
\value{
A list of \code{transport_option} S3 objects, sorted by the user's
travel style.
}
\description{
Tries the provider implied by \code{transport}:
\itemize{
\item \code{plane} -> Aviationstack (schedules) if \code{AVIATIONSTACK_KEY} is
set, otherwise Amadeus Self-Service if \code{AMADEUS_CLIENT_ID/SECRET}
are set, otherwise mock data.
\item \code{train} -> koleo.pl public endpoints (currently flaky -> mock)
\item \code{bus}/\code{car} -> deterministic synthetic estimates
}
If a network call fails (no API key, offline, rate-limit) the function
degrades gracefully to a deterministic mock so the rest of the pipeline
keeps working — the Shiny UI can still demonstrate behaviour without
credentials.
}
