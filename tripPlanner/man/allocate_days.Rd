% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importance.R
\name{allocate_days}
\alias{allocate_days}
\title{Allocate a fixed number of trip days across cities by importance.}
\usage{
allocate_days(
  cities,
  total_days,
  min_days = 1L,
  weights = c(population = 0.15, historical = 0.35, cultural = 0.35, poi = 0.15)
)
}
\arguments{
\item{cities}{data.frame with columns: city (or name), population,
historical_score, cultural_score, poi_count.}

\item{total_days}{Integer trip length in days (>= n_cities).}

\item{min_days}{Minimum days per city (default: 1).}

\item{weights}{Forwarded to [calculate_importance()].}
}
\value{
A data.frame with columns city, importance, days.
}
\description{
Each city receives `min_days` to start, then the remaining days are
distributed proportionally to its share of total importance. Any rounding
residual is added to the most important city so the totals match.
}
